---
title: Projects
type: project
tags:
  - operations
created: {{DATE}}
updated: {{DATE}}
---

# Projects

## Active

_No active projects yet. Each project lives under `projects/<project-name>/` with `state.md` and `plan.md`. Add a link here when starting one._

## Archive

_Empty. Move completed projects to `projects/archive/` and list them here._

## Bootstrapped by ingest

The `meeting-ingest` skill auto-bootstraps a `plan.md` when a meeting flags an action candidate against a project that doesn't yet have one. The minimal shape: frontmatter + `# Plan` heading + `## Action candidates (auto-flagged)` section. The first time you see one of these, fill in the rest.
